# Project Information
pluginGroup=Randomizer
pluginVersion=1.0.0
pluginDescription=A Hytale plugin to roll random numbers with penertration (max roll, roll again and add result -1, continue until no longer max roll)

# Gradle Configuration
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=true
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
